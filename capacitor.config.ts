import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.com.insignia.vault',
  appName: 'vault-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
